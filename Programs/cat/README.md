# How to use

Compile to jar with custom name  
`niva --out-name=cat build`  
Run
```bash
java -jar cat.jar README.md                                                                                                                                                                                                                                                                 main ✱
# How to use

Compile to jar with custom name  
`niva --out-name=cat build`  
Run
```